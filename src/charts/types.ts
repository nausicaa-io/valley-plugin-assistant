// Chart theme + data types — ported from the reference implementation types/charts.ts (chart subset).

export interface TextColorTheme {
  bodyColor: string
  bodyColorText: string
  titleColorRGBA: string
  lineColorRGBA: string
  textColorRGBA?: string
  backgroundColor?: string
}
